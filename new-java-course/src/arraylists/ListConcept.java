package arraylists;

/*
    List is an interface
    ArrayList --> ordered collection
    Resizable-array implementation of the List interface --> Equivalent to Vector but unsynchronized

    fail-fast:if the list is structurally modified at any time after the iterator is created, in any way except
     through the iterator's own remove or add methods, the iterator will throw a ConcurrentModificationException

     https://docs.oracle.com/javase/8/docs/api/java/util/ArrayList.html
 */
public class ListConcept {
}
